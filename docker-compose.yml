services:
  ic-seeker:
    build: .
    ports:
      - "${PORT:-8750}:8750"
    env_file:
      - .env
    environment:
      HOST: 0.0.0.0
      PORT: 8750
      IC_SEEKER_DB: /app/ic_database/ic_papers.sqlite
      IC_SEEKER_CSV: /app/ic_database/ic_chipseeker.csv
      IC_SEEKER_PDF_INBOX: /app/ic_database/pdf_inbox
    volumes:
      - ./ic_database:/app/ic_database
    restart: unless-stopped
